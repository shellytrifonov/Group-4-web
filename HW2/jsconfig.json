{
  "compilerOptions": {
    "baseUrl": ".", // Set the root directory of your project
    "paths": {
      "@/*": ["./*"] // Map the alias "@/..." to the root folder
    }
  },
  "exclude": ["node_modules"]
}
